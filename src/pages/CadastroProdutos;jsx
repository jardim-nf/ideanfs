import { useNavigate } from 'react-router-dom';

export default function CadastroProdutos() {
  const navigate = useNavigate();

  return (
    <div className="min-h-screen bg-gray-100 p-8">
      <div className="max-w-4xl mx-auto bg-white rounded-xl shadow border p-8">
        <button onClick={() => navigate('/dashboard')} className="text-blue-600 font-bold mb-6">← Voltar</button>
        
        <h2 className="text-2xl font-bold mb-6">Novo Serviço / Produto</h2>
        
        <form className="space-y-6">
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
            <div className="md:col-span-2">
              <label className="block text-sm font-bold mb-1">Nome do Serviço</label>
              <input type="text" className="w-full p-2 border rounded" placeholder="Ex: Desenvolvimento Web" />
            </div>
            <div className="md:col-span-1">
              <label className="block text-sm font-bold mb-1">Valor Padrão (R$)</label>
              <input type="text" className="w-full p-2 border rounded" placeholder="0,00" />
            </div>
            <div className="md:col-span-3">
              <label className="block text-sm font-bold mb-1">Descrição Detalhada Padrão</label>
              <textarea rows="3" className="w-full p-2 border rounded" placeholder="Essa descrição sairá na nota fiscal..."></textarea>
            </div>
          </div>

          <div className="bg-gray-50 p-4 border rounded grid grid-cols-2 gap-4">
            <div>
              <label className="block text-sm font-bold mb-1">Código do Serviço (LC 116)</label>
              <input type="text" className="w-full p-2 border rounded" placeholder="01.01" />
            </div>
            <div>
              <label className="block text-sm font-bold mb-1">Alíquota ISS (%)</label>
              <input type="number" step="0.01" className="w-full p-2 border rounded" placeholder="3.00" />
            </div>
          </div>

          <button type="button" onClick={() => alert('Em breve conectaremos com o Firebase Firestore!')} className="bg-blue-600 text-white font-bold py-3 px-8 rounded mt-4">
            Salvar Serviço
          </button>
        </form>
      </div>
    </div>
  );
}